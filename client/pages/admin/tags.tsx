import type { ReactElement } from 'react';

export default function AdminTags(): ReactElement {
  return (
    <main className="admin-tags-page">
      <h1>タグ管理</h1>
      <p>ここにタグ一覧と編集・削除機能を実装します。</p>
    </main>
  );
}
