import type { ReactElement } from 'react';

export default function AdminDenyIps(): ReactElement {
  return (
    <main className="admin-deny-ips-page">
      <h1>IP 制限管理</h1>
      <p>ここに IP 制限の追加・削除機能を実装します。</p>
    </main>
  );
}
