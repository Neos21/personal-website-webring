import type { ReactElement } from 'react';

export default function Edit(): ReactElement {
  return (
    <main className="edit-page">
      <h1>編集・削除</h1>
      <p>ここに自薦サイトの編集・削除フォームを実装します。</p>
    </main>
  );
}
