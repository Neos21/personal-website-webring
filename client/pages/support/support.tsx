import type { ReactElement } from 'react';

export default function Support(): ReactElement {
  return (
    <main className="support-page">
      <h1>サポート掲示板</h1>
      <p>ここに掲示板投稿一覧と投稿フォームを実装します。</p>
    </main>
  );
}
