# TASKS.md

## 1. 目的
README.md の `## コンセプト` に記載された機能を、既存の client / server 構造をベースに順番に実装する。

## 2. フェーズとステップ

### フェーズ 1: クライアント / サーバーのスキャフォールド
- [x] pages に必要なページファイルを作成する
  - `/`, `/new`, `/list`, `/site`, `/edit`, `/support`
  - `/admin`, `/admin/dashboard`, `/admin/sites`, `/admin/tags`, `/admin/support`, `/admin/deny-ips`
- [x] routes.ts に全ルートを追加する
- [x] routes に以下のルートを追加する
  - `/prev`, `/next`, `/random`
  - `/api/sites`, `/api/sites/:id`, `/api/sites/:id/comments`
  - `/api/posts`
  - `/api/admin/*`
- [x] 既存の sites.ts を API スタブに拡張する
- [x] 共通の型 / スキーマ定義を追加する

### フェーズ 2: データモデルとサーバー実装
- [x] README.md の DB スキーマを参照して D1 用のクエリ/ヘルパーを実装する
- [x] URL 正規化ロジックを実装する
  - 完全一致は登録不可
  - 近い URL は警告表示のみで登録可能
- [x] タグ正規化ロジックを実装する
  - 大文字小文字を区別せず、既存タグ名にマージする
- [x] サイト登録 API を実装する
  - 他薦/自薦の入力差分を反映
  - 他薦登録時は推薦コメントを `site_comments` に追加
  - 自薦登録時は `password_hash` を保存
- [x] サイト更新 / 論理削除 API を実装する
  - 自薦サイトの編集・削除
  - 他薦サイトの「自薦切り替え」対応
- [x] サイトコメント API を実装する
- [x] サポート掲示板 API を実装する
- [x] `/prev`, `/next`, `/random` のリダイレクトロジックを実装する
- [x] IP 制限 / 管理画面 API の基礎を追加する

### フェーズ 3: クライアント UI の実装
- [x] `/new` フォームを実装する
  - 他薦 / 自薦の切り替え
  - フィールド要件と最大文字数
  - バナーサイズ選択
  - Turnstile ウィジェットのプレースホルダ
- [ ] `/list` ページを実装する
  - 登録日時順、ページング（100件ずつ）
- [ ] `/site` ページを実装する
  - サイト詳細、コメント一覧、コメント投稿
  - `/support?id=...` への遷移リンク
- [ ] `/edit` ページを実装する
  - 自薦サイト編集・削除フォーム
  - 他薦サイトの「このサイトの管理人ですか？」フロー
- [ ] `/support` ページを実装する
  - 掲示板投稿一覧
  - 投稿フォーム
- [ ] `/admin` 系ページを実装する
  - 管理ログイン
  - サイト管理
  - タグ管理
  - サポート投稿管理
  - IP 制限管理

### フェーズ 4: バリデーションと動作確認
- [ ] README.md にある入力制限をすべて反映する
- [ ] バナー URL あり時のサイズ必須制御
- [ ] 近い URL は警告表示のみで許可
- [ ] コメント投稿後の編集/削除不可
- [ ] 管理画面の操作フロー確認

### フェーズ 5: 最終検証
- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] 手動で主要ページと API を確認
- [ ] README.md に記載のファイル/ルート構成と合致していることを確認
