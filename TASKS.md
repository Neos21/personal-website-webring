# TASKS.md

@AGENTS.md を遵守しながら実装すること。


## サイトの新規登録フォーム、編集フォームの改善

- [x] URL 入力欄 : 各種 SNS (Twitter・Instagram 等) やレンタルブログ (note・Zenn・Qiita 等) のドメインは登録不可とする (個人サイトに該当しないと考えるため)
  - [x] URL 入力欄で Blur した際、GET `/api/deny-domains/search` をコールする。禁止ドメインに合致したら「このドメインは登録できません」エラーメッセージを動的に表示する
  - [x] 登録処理・編集処理の中でも禁止ドメインに合致しているかどうかをチェックする
- [x] URL 入力欄 : 完全一致する URL、類似する URL の判定を、Blur 時に動的に行う (現状、登録処理・編集処理の中では `SiteUrlService#findSiteUrlMatch()` で判定している)
  - [x] URL 入力欄で Blur した際、GET `/api/sites/search-url` をコールする
  - [x] 完全一致するサイトがあれば「この URL は登録済みです : ID [2]」エラーメッセージを動的に表示し、登録不可にする。ID 部分はリンクにし、`/site?id=【ID】` に遷移できるようにする
  - [x] 類似するサイトがあれば「類似する URL が登録済みです : ID [2]」ワーニングメッセージを動的に表示する。ID 部分はリンクにし、`/site?id=【ID】` に遷移できるようにする。登録自体は続行可能にする
- [x] タグ入力欄を、より入力、削除しやすい UI にする
  - [x] スペースを含めたタグを登録できるようにしたい。`[ テキストボックス ] [追加]` と並んでいる UI にして、タグを1つずつ Enter or 追加ボタン押下で登録できるようにする
  - [x] テキストボックス・追加ボタンの下に、登録したタグをラベル風にした `button` 要素で並べる。`[タグ1 ×]　[タグ2 ×]` というように羅列し、そのタグ (ボタン) を押下すればタグを削除できるようにする
  - [x] 編集フォームの場合も、登録済みのタグをラベル風ボタンで列挙した状態で初期表示できるようにする
  - [x] 追加済みのタグが10個に到達したら、テキストボックス・追加ボタンは非活性にする


## リングマスター用管理画面 (`/admin` 配下) を実装する

- [x] 管理ログイン
- サイト管理
  - [x] 一覧表示ページ : 500件でページング可能にする (`shared/constants/admin.ts` に `export const adminConstants = { sitesPageSize: 500 }` を作る)
  - [ ] 1件の詳細表示ページ : 全項目の編集 (論理削除フラグの ON・OFF 込み) を可能にする。物理削除を可能にする。(`site_tags` テーブルの連動削除も行う)
- タグ管理
  - [x] 一覧ページ : 500件でページング可能にする (`adminConstants.tagsPageSize: 500` を用意する)
  - [ ] `[ タグ名 ]　　[編集] [削除]` とテキストボックス、ボタンを横並びにして1行を構成する。タグ名の表記を書き換えて「編集」ボタンを押せばその場で更新できる
  - [ ] 「削除」ボタンを押せばそのタグを削除できる。そのタグが紐付いているサイトが存在している場合は削除不可にする
- サポート投稿管理
  - [x] 一覧ページ : 500件でページング可能にする (`adminConstants.postsPageSize: 500` を用意する)。一覧ページ上部に「管理者投稿フォーム」を用意する
  - [ ] 投稿1件の詳細表示ページ : その投稿の全項目の編集を可能にする。物理削除を可能にする
- IP 制限管理
  - [x] 一覧ページ : 新規追加と、登録済みのものの削除を可能にする
  - [ ] IPv6 アドレスを新規登録する場合は、完全展開したアドレスの上位64ビットまで (例 : `2001:0db8:0000:0000::/64` 形式) に変換して登録する (`convertIpV6AddressTo64Bit` 関数を利用)
  - [ ] これに伴い、既存の `new DenyIpsRepository(context.env.DB).isIpDenied(ip)` で判定している部分を IPv6 の64ビットでチェックするよう対応する
- 登録禁止ドメイン管理
  - [ ] 一覧ページ : 新規追加と、登録済みのものの削除を可能にする


## サイト探索機能の追加

- [ ] サイト名、管理人名、説明文、URL を対象項目にした検索ページ : 結果一覧はページング機能付き
- [ ] タグ一覧を表示し、タグを押下するとタグが紐付いているサイトを列挙する : 結果一覧はページング機能付き
  - [ ] タグ一覧では `[タグ1 (15)]　[タグ2 (4)]` のように、タグに紐付いているサイト数をカッコで表示したい
  - [ ] タグ自体の検索を可能にする
